# Freelance Project Manager — Dependencies
# No third-party packages required.
# The application uses only the Python standard library:
#   - tkinter   (GUI)
#   - sqlite3   (Persistence)
#   - abc       (Abstract base classes)
#   - datetime  (Date/time handling)
#   - re        (Email validation)
#
# Requires: Python >= 3.10
